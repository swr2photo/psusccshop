import type { ReactNode } from 'react';

export const metadata = {
  title: 'ใบเสร็จ | SCC Shop',
  robots: { index: false, follow: false },
};

export default function ReceiptLayout({ children }: { children: ReactNode }) {
  return children;
}
