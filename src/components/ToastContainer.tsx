'use client';

/**
 * Legacy toast UI removed — app-wide notifications use
 * `@/components/ui/toast` (Base UI + shadcn Toaster).
 * Kept as a stub so old lazy imports do not crash.
 */
export default function ToastContainer() {
  return null;
}
